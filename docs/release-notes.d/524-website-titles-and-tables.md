- The preview website's pages have page-first browser titles (such as
  `Sources | readmit`), a Skip to content link, a navigation landmark named
  Main navigation, and shorter card, section, table and workflow labels
  (#524). Every table has a caption, column and row headers, and scrolls
  sideways inside its own keyboard-focusable region on a narrow screen instead
  of hiding a column. No command, transcript, version, bound, warning or
  capability or release claim changed.
