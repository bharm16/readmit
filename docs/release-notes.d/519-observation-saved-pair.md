- Observations now open a source or window file only when you press Open
  source or Open window, and ask before replacing unsaved edits. The panel is
  split into Source, Completion rules, and Collect and results views, with the
  saved source and window IDs always in view. Collect once collects only the
  clean saved pair you authorized: it stays closed while the editor has unsaved
  edits or a save wrote only one document, and a saved file changed on disk
  since you authorized it is refused before anything is collected (#519).
  Database filters are now listed rows you add, edit and remove, and the
  Completion rules view edits a recorded baseline's ID and the maximum records
  and samples a collection takes.
