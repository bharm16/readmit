- The HL7 file inspector and the performance corpus screen use shorter,
  plainer labels (#523). The inspector's choices are grouped as **Input
  format**, with captions such as **Detect automatically**, **Raw HL7** and
  **MLLP frames**; **Show values** and its patient-data warning sit outside
  that group, and the byte-identical copy is its own section below the result.
  The corpus screen shows **Generate** and **Scan** one at a time, keeps what
  you typed in each, keeps a running generation or scan and its cancel button
  in view while you switch, and moves the batch-size overrides under
  **Advanced scan limits**. The values sent, the defaults and the results are
  unchanged.
