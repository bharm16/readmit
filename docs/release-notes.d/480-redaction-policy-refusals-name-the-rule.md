- Disclosure policy refusals now name the failing rule: an unsupported `readmit-redact-policy/v1` document is refused with the specific declaration that failed — the field rule and its policy, the patient scope, the segment list, the spec binding or the required failure it declares — instead of one generic sentence, in `readmit redact` and in the desktop disclosure policy panel. Which policy documents are accepted is unchanged. Refusals identify invalid entries by position without echoing unvalidated document text.
