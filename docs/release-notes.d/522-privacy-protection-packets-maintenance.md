- The privacy, protection, packet and maintenance screens use clearer names
  and separate tasks (#522). Disclosure policy rows show readable data
  categories and treatments while the saved policy keeps its exact versioned
  values, and changing a review's inputs withdraws its approval. A protection
  package's control is chosen only from the protection file named for that
  package, draft package contents can be removed before packing, opening a
  package lists its plaintext output, and discarding one asks first, naming the
  package and its retention. Maintenance rebuilds an index through the same
  setup as the explorer, keeping that index's own fields, stored content and
  retention end, starts quota limits from the project's declared quota and
  saves only whole numbers, and Recover document names the one document and
  copy it replaces.
